[project]
name = "telos"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langgraph",
]

[project.scripts]
telos = "telos.main:main"

[build-system]
requires = ["uv_build>=0.10.7,<0.11.0"]
build-backend = "uv_build"
